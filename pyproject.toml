[tool.poetry]
name = "mazegen"
version = "1.0.0"
description = "A maze generator and solver library"
packages = [{include = "mazegenerator"}]

[tool.poetry.dependencies]
python = ">=3.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"